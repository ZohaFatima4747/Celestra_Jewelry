# Leave empty to use Vite's built-in proxy (recommended for local dev)
# Set to your deployed backend URL for production builds
VITE_API_BASE_URL=

# Public site URL — used for canonical URLs, OG tags, and structured data
# Set this to your production domain before deploying
VITE_SITE_URL=https://celestraa.com
